#include <iostream>
#include <iomanip>
using namespace std;
int main(){
    //use of const keyword
    int a=22,b=1213;
    //manipulators (endl,\n,setw etc.) in c++
    cout<<"The value of a is "<<setw(4)<<a<<endl;
    cout<<"The value of b is "<<setw(4)<<b<<endl;
    //operator precedence(priority)and associativity(L to R/R to L)  check it on cppreference sites 
    
    return 0;
}